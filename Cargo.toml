[package]
name = "sparkfun-otos"
version = "0.1.0"
edition = "2024"
license = "LGPL-3.0-only AND MIT"

[dependencies]
defmt = {version = "1.1.0", optional = true}
embedded-hal-async = "1.0.0"

[features]
defmt = ["dep:defmt"]
